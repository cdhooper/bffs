; This script will build ziptest using the current (September 2017) version
; of vbcc.  The vbcc_config file contains other required options.

vc -sc -v -lamiga -+ -Iinclude:amiga20 -I../ucbinclude -Dcdh -DAMIGA ls.c -o ls
